export const config = {
    apiUrl: import.meta.env.VITE_API_URL,

    // Guest account credentials
    guestEmail: import.meta.env.VITE_GUEST_ACCOUNT_EMAIL,
    guestPassword: import.meta.env.VITE_GUEST_ACCOUNT_PASSWORD,
};
